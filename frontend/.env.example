# Base URL of the Express API. Must include the /api/v1 suffix.
VITE_API_URL=http://localhost:5000/api/v1

# Optional — set this (and GOOGLE_CLIENT_ID on the backend) to show the
# "Continue with Google" button on the login screen.
VITE_GOOGLE_CLIENT_ID=
